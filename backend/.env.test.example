# whaviso — template do env de TESTE (vitest). Copie para .env.test e preencha.
# Mantém os testes de integração 100% no Postgres LOCAL (whaviso_dev), isolando-os
# do ./.env — que aponta o runtime de dev (api/zap) para o Supabase cloud.

POSTGRES_PASSWORD=
PGHOST=127.0.0.1
PGDATABASE=whaviso_dev
