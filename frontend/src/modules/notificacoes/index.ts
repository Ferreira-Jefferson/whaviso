// Diferente dos demais módulos (que só exportam páginas lazy): o sino é um componente do
// HEADER COMUM (AppShell), sempre montado, não uma rota; exportado direto (sem lazy).
export { SinoNotificacoes } from './components/SinoNotificacoes'
